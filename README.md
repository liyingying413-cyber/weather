# Weather on Map — Open‑Meteo + streamlit-folium

- Click a city on the **map** to load live weather
- Or search by name in the sidebar
- No API key required (Open‑Meteo)
- Includes a soft weather background image

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```

